{{#if logged_in}}
<a href="/newrecipe">
<div class="card-body">
    <h6>Add new recipe</h6>
</div>
</a>

{{#each userRecipes as |recipe| }}
<h1>Recipe</h1>
<img src="{{recipe.image}}"></h1>
<p>{{recipe.description}}</p>
{{/each}}

{{/if}}